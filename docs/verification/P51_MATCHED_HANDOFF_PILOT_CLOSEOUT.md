# P5.1 matched handoff pilot closeout

The user-reviewed four-slot pilot is completed and closed. The accepted
post-study disposition is **Narrow — no comparative G usefulness advantage
established**. Repository product/Core implementation remains
`no_change_needed_with_evidence`. This records two matched synthetic cases,
not four independent cases. Current and next work are owned solely by the
[roadmap](../vnext/03_AUGNES_VNEXT_TRANSITION_ROADMAP.md#p1p5-status-and-next-decision).

## Evidence binding and comparison

The completed study binds application source
`9cb9e43449a470017cebbff8d099416b5d9ba5f2`, tree
`f24f74bfca8d083a03816f288f647f2cc087b924`, and execution review-index SHA-256
`366fab078fdc1d95e61e05d6c1f9b5d5a1714abb9b345cafd0cb6ed6cd787ade`.
Its task-information cutoff was `2026-09-12T05:27:15.000Z`; execution occurred
on 2026-09-12. Detailed evidence remains private. These historical bindings
do not verify a later documentation head.

F delivered the frozen strong free note; G delivered the frozen governed
selected-source handoff. Each matched pair received the same two synthetic
source files, substantive facts, corrections, uncertainties, task, cutoff,
permissions and common mandatory Augnes safety/authority envelope. F remained
inside that envelope. This was neither a no-Augnes control nor a comparison
of accepted ReviewDecision/Transition histories.

G used four governed entries where F used one note, adding provenance,
presentation and validation differences. This is a handoff-system comparison,
not a pure representation effect. Both used the same qualified ordinary
native-managed host, model `gpt-6-astra`, effort `max`, equal context ceilings
and scoped controls; effective settings were checked per invocation. Strict
HOLD was unchanged.

## Outcomes in the fixed execution order

All four executions completed and durably settled. All four receipt
verification values remain **partial**.

| Slot | Assignment | Source-grounded task result | Consequential condition |
|---|---|---|---|
| 1 | rate-scope / F | 10/5000 = 0.20%; rate minus ceiling −0.05 percentage points; inclusive 0.25% ceiling met | Warm-up excluded; historical count-only correction retention unknown |
| 2 | rate-scope / G | 10/5000 = 0.20%; explicitly ceiling minus rate +0.05 percentage points; inclusive ceiling met | Warm-up excluded; historical count-only correction retention unknown |
| 3 | resolved-denominator / G | Revision 2: 6/4000 = 0.15%; rate minus ceiling −0.10 percentage points; inclusive ceiling met | Supplied denominator resolution admitted for this file; obsolete draft legitimately superseded |
| 4 | resolved-denominator / F | Revision 2: 6/4000 = 0.15%; rate minus ceiling −0.10 percentage points; inclusive ceiling met | Supplied denominator resolution admitted for this file; obsolete draft legitimately superseded |

Both conditions produced correct rates and inclusive-ceiling decisions. Both
resolved-denominator outputs made legitimate scoped progress without asking
for the supplied denominator. No semantic false blocking was observed in the
four outputs. Unknown cause, untested South/other applicable periods, and the
synthetic source basis remained distinct from live or accepted Core evidence.

Rate-scope G does not meet the frozen key's rate-minus-ceiling convention.
Its explicitly stated inverse margin is mathematically coherent, and the
worker-facing task did not specify subtraction order. Preserve the strict
key mismatch without calling it clean evidence of arithmetic failure or a G
effect. Neither rate-scope output establishes retention of the historical
count-only correction: correct current arithmetic alone does not establish
that retention. It remains unsupported/unknown, neither a pass nor proven
loss. The frozen task, key, scores and records are unchanged.

Evaluation uses the existing retained / demonstrably resolved / legitimately
superseded / unsupported-or-unknown distinctions in the
[evaluation owner](../vnext/04_AUGNES_VNEXT_EVALUATION_AND_MATURITY.md#cumulative-preservation-of-still-valid-conditions).
These are report distinctions, not new Core states. Native completion,
packet delivery, receipt verification, criterion-level task scoring, proposal
recording and semantic acceptance remain separate. The selected-source machine
`required_checks` arrays were empty; external task evaluation was necessary.

## Execution, separation and evidence limits

Four attempt records preceded four direct executor calls. Four App Server
launch events and four task turns were observed. Forty version probes are
source-derived from the completed path, not independently observed subprocess
counts or a total native/helper-process count. No predecessor, evaluator-model,
calibration, retry, replacement, resume or additional study call occurred in
the completed dispatch.

Each invocation used fresh migrated disposable owners and a genuine fresh
snapshot. Source/snapshot integrity passed; all owned snapshots were released,
sessions revoked and database handles closed after durable settlement. Four
pending proposals arose through normal result/receipt recording; none was
accepted. No ReviewDecision, Transition or semantic application occurred.
Cleanup does not replace receipt-level settlement evidence.

Supported input/configuration checks found no material leakage or drift.
Sibling outputs, evaluator-only material and historical X/B/Y were outside
worker inputs. Fresh databases/threads do not prove cold isolation. The
preparer knew the conditions and was not an independent blinded evaluator.
Reading bytes, calculation methods and task actions remain host-attested
except where specific process evidence exists; correct arithmetic is not
independently observed file-byte consumption. Backend/hidden serving state,
provider rounds, tokens, attributed cost and total low-level process counts
remain unknown.

## Burden and bounded operational observations

| Assignment | Packet / GuideBrief serialized bytes | Recorded executor envelope (ms) |
|---|---:|---:|
| rate-scope / F | 20,827 / 2,466 | 64,705 |
| rate-scope / G | 25,091 / 2,688 | 80,233 |
| resolved-denominator / G | 25,261 / 2,688 | 75,743 |
| resolved-denominator / F | 20,997 / 2,466 | 71,037 |

G had larger local serialized payloads and longer executor envelopes in these
two ordered pairs. These are not wire-byte or provider-token measurements and
do not establish causal slowdown, comparative operating cost, human savings,
general equivalence or a token/cost advantage.

Recurring caller wall time was 296,860 ms, including 1,391 ms shared setup.
Per-slot fresh preparation took 820/800/794/783 ms and post-executor cleanup
101/103/109/104 ms in slot order. Gate, startup, retrieval and recovery times
are nested in executor envelopes and must not be added again. Selection,
active human/agent labor and separately attributable retrieval/validation
effort were unmeasured. One-time design, qualification and evaluation setup
remain separate from recurring operation; the original 107/84/82/72 ms offline
readings do not show human savings. Additional PAYG authority of $0 is not
evidence of zero cost.

All four outputs reported blocked instruction/validation discovery and then
completed the calculation. One marked skipped repository validation as
required; its downstream effect is untested. Reported command-failure recovery
and rereading are not automatically semantic false blocking or unnecessary
repetition. This is a bounded follow-up observation, not an established owner
defect or authority to widen access, remove discovery rules, change receipt
logic or add a mandatory readiness phase.

## Disposition and preservation

Narrow follows from limited/mixed condition evidence and incomplete burden
measurement. Both routes supported scoped progress; neither pair establishes
a concrete G usefulness advantage. No Go claim is supported, and no observed
material leakage or source/model/authority mismatch invalidated the whole
comparison. The prospective contract and observable-retention lessons live in
the [evaluation owner](../vnext/04_AUGNES_VNEXT_EVALUATION_AND_MATURITY.md#worker-facing-contracts-and-observable-retention).

The original freezes, both earlier stopped dispatches, completed dispatch,
historical failures and consumed allowances remain intact. Historical P1,
P5.1a and bounded live-loop completion are preserved; X/B/Y was not rerun or
used as new held-out evidence. This documentation closeout adds no study,
runtime, billing, adoption, deployment or merge authority.
