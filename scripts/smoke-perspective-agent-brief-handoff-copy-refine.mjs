import assert from "node:assert/strict";
import { execFileSync } from "node:child_process";
import { existsSync, readFileSync } from "node:fs";

const packageFile = "package.json";
const builderFile =
  "lib/perspective-ingest/perspective-agent-brief-handoff-packet.ts";
const dogfoodScriptFile =
  "scripts/dogfood-perspective-manual-agent-brief-codex-review-loop.mjs";
const smokeFile =
  "scripts/smoke-perspective-agent-brief-handoff-copy-refine.mjs";
const docFile =
  "docs/PERSPECTIVE_AGENT_BRIEF_HANDOFF_COPY_REFINE_V0_1.md";
const reportFile =
  "reports/2026-06-07-perspective-agent-brief-handoff-copy-refine.md";
const dogfoodPacketFile =
  "reports/dogfood/2026-06-07-perspective-manual-agent-brief-codex-review-loop-packet.md";
const dogfoodEvalReportFile =
  "reports/2026-06-07-perspective-manual-agent-brief-codex-review-loop-eval.md";
const codexReviewLoopDocFile =
  "docs/PERSPECTIVE_MANUAL_AGENT_BRIEF_CODEX_REVIEW_LOOP_EVAL_V0_1.md";
const codexReviewLoopSmokeFile =
  "scripts/smoke-perspective-manual-agent-brief-codex-review-loop-eval.mjs";
const handoffDogfoodSmokeFile =
  "scripts/smoke-perspective-manual-agent-brief-handoff-dogfood.mjs";
const manualIngressContextSmokeFile =
  "scripts/smoke-perspective-agent-brief-manual-ingress-context.mjs";
const readSurfaceSmokeFile =
  "scripts/smoke-perspective-agent-brief-read-surface.mjs";
const localManualSmokeFile =
  "scripts/smoke-perspective-local-manual-ingress-admission-preview.mjs";
const ingressModelSmokeFile =
  "scripts/smoke-perspective-ingress-admission-model.mjs";
const projectionBuildersSmokeFile =
  "scripts/smoke-perspective-temporal-spatial-projection-builders.mjs";
const workbenchSmokeFile =
  "scripts/smoke-cockpit-perspective-workbench-temporal-underlay.mjs";
const handoffPacketStructureSmokeFile =
  "scripts/smoke-perspective-handoff-packet-structure-review.mjs";
const cockpitFile = "components/augnes-cockpit.tsx";

const packageJson = JSON.parse(readFileSync(packageFile, "utf8"));
const builderText = readFileSync(builderFile, "utf8");
const docText = readFileSync(docFile, "utf8");
const cockpitText = readFileSync(cockpitFile, "utf8");
const handoffPacketStructureSmoke = readFileSync(
  handoffPacketStructureSmokeFile,
  "utf8",
);

const { buildPerspectiveAgentBrief } = await import(
  "../lib/perspective-ingest/perspective-agent-brief.ts"
);
const { buildPerspectiveAgentBriefHandoffPacket } = await import(
  "../lib/perspective-ingest/perspective-agent-brief-handoff-packet.ts"
);
const { buildPerspectiveIngestLocalPreviewReadResponse } = await import(
  "../lib/readonly-api/perspective-ingest-local-preview.ts"
);
const { buildPerspectiveAgentBriefSourcePreview } = await import(
  "../lib/readonly-api/perspective-agent-brief.ts"
);
const {
  PERSPECTIVE_MANUAL_AGENT_BRIEF_CODEX_REVIEW_LOOP_INPUT,
  getPerspectiveManualAgentBriefCodexReviewLoopForbiddenMarkers,
  getPerspectiveManualAgentBriefCodexReviewLoopForbiddenValues,
  runPerspectiveManualAgentBriefCodexReviewLoopDogfood,
} = await import("./dogfood-perspective-manual-agent-brief-codex-review-loop.mjs");

const expectedTsxCommand =
  "./apps/augnes_apps/node_modules/.bin/tsx --tsconfig tsconfig.json";
const allowedChangedFiles = new Set([
  packageFile,
  builderFile,
  dogfoodScriptFile,
  smokeFile,
  docFile,
  reportFile,
  dogfoodPacketFile,
  dogfoodEvalReportFile,
  codexReviewLoopDocFile,
  codexReviewLoopSmokeFile,
  handoffDogfoodSmokeFile,
  manualIngressContextSmokeFile,
  readSurfaceSmokeFile,
  localManualSmokeFile,
  ingressModelSmokeFile,
  projectionBuildersSmokeFile,
  workbenchSmokeFile,
]);

assert.equal(
  packageJson.scripts["smoke:perspective-agent-brief-handoff-copy-refine"],
  `${expectedTsxCommand} ${smokeFile}`,
  "package.json must register smoke:perspective-agent-brief-handoff-copy-refine",
);

for (const file of [builderFile, dogfoodScriptFile, smokeFile, docFile]) {
  assert.equal(existsSync(file), true, `${file} must exist`);
}

assertContainsAll(docText, [
  "# Perspective Agent Brief Handoff Copy Refine v0.1",
  "refines the copy generated by `buildPerspectiveAgentBriefHandoffPacket`",
  "follows PR #455",
  "packet is context only",
  "Codex may code, test, and open a PR only when the surrounding user prompt explicitly scopes that task",
  "Codex must not merge, deploy, publish, or approve",
  "ChatGPT reviews the PR",
  "The user decides whether to merge",
  "why “Do not execute Codex” was replaced",
  "raw pasted text",
  "raw `ingress_admission` JSON",
  "candidate/source/pointer/actor/consent values",
  "does not add routes",
  "modify `app/api`",
  "preserves graph topology",
  "existing Perspective Handoff packet section order",
  "Add reviewed manual Agent Brief packet template for Codex prompts",
]);

assertContainsAll(builderText, [
  "buildPerspectiveAgentBriefHandoffPurpose",
  "buildPerspectiveAgentBriefHandoffConstraints",
  "buildPerspectiveAgentBriefHandoffAuthority",
  "Local read-only Agent Brief handoff for a user-approved Codex PR workflow.",
  "Codex may code, test, and open a PR only when the surrounding prompt explicitly scopes that task.",
  "This packet does not grant merge, deploy, provider, persistence, Formation, or unscoped execution authority.",
  "Do not merge, deploy, publish, or approve.",
  "Do not mutate GitHub except opening the scoped PR when explicitly requested.",
  "Do not call providers/models/APIs.",
  "ChatGPT reviews the PR.",
  "User decides whether to merge.",
  "Packet does not grant Codex execution authority by itself.",
]);
assert.equal(
  builderText.includes("\"Do not execute Codex.\""),
  false,
  "builder must not contain the ambiguous standalone Codex execution ban",
);

const generatedAt = "2026-06-08T00:00:00.000Z";
const manualPreview = buildPerspectiveIngestLocalPreviewReadResponse({
  generatedAt,
  request: {
    input_kind: "manual:pasted_text",
    source_label: "Agent Brief copy refine smoke",
    input_text: [
      "Intent: Validate refined Agent Brief handoff copy.",
      "Concept: Copy should distinguish packet context from user-approved Codex PR work.",
      "Decision: Keep the builder pure and read-only.",
      "Work: Smoke the handoff audiences and dogfood artifact.",
      "Changed: copy-only packet builder text.",
      "Validation: Check refined constraints, authority, and raw-value omissions.",
      "Evidence: agent-brief-copy-refine-smoke",
    ].join("\n"),
  },
});
const manualBrief = buildPerspectiveAgentBrief({
  preview: manualPreview,
  scope_mode: "whole_constellation",
  scope_label: "Whole Constellation",
});

const codexPacket = buildPerspectiveAgentBriefHandoffPacket({
  brief: manualBrief,
  audience: "codex_handoff",
  generated_at: generatedAt,
});
const chatGptPacket = buildPerspectiveAgentBriefHandoffPacket({
  brief: manualBrief,
  audience: "chatgpt_review",
  generated_at: generatedAt,
});
const agentPacket = buildPerspectiveAgentBriefHandoffPacket({
  brief: manualBrief,
  audience: "agent_context",
  generated_at: generatedAt,
});

assertContainsAll(codexPacket.packet_text, [
  "user-approved Codex PR workflow",
  "code, test, and open a PR",
  "surrounding prompt explicitly scopes",
  "This packet does not grant",
  "Do not merge, deploy, publish, or approve.",
  "ChatGPT reviews the PR.",
  "User decides whether to merge.",
  "Packet authority: context only.",
  "User-approved PR workflow required for Codex code/test/open-PR work.",
  "Packet does not grant Codex execution authority by itself.",
  "No GitHub mutation outside explicitly scoped PR creation.",
]);
assert.equal(
  codexPacket.packet_text.includes("- Do not execute Codex."),
  false,
  "codex_handoff packet must not include the ambiguous standalone Codex execution ban",
);
assertSectionOrder(codexPacket.packet_text);

assertContainsAll(chatGptPacket.packet_text, [
  "ChatGPT and human review or planning",
  "not as an implementation instruction by itself",
  "No GitHub mutation authority.",
]);
assert.equal(
  chatGptPacket.packet_text.includes("- Do not execute Codex."),
  false,
  "chatgpt_review packet must not include the ambiguous standalone Codex execution ban",
);
assertSectionOrder(chatGptPacket.packet_text);

assertContainsAll(agentPacket.packet_text, [
  "structured agent reasoning",
  "should not trigger side effects",
  "Use for read-only context only.",
  "No GitHub mutation authority.",
]);
assert.equal(
  agentPacket.packet_text.includes("- Do not execute Codex."),
  false,
  "agent_context packet must not include the ambiguous standalone Codex execution ban",
);
assertSectionOrder(agentPacket.packet_text);

const dogfood = runPerspectiveManualAgentBriefCodexReviewLoopDogfood();
assert.equal(existsSync(dogfoodPacketFile), true, "dogfood packet artifact must exist");
assert.equal(existsSync(dogfoodEvalReportFile), true, "dogfood eval report must exist");

const dogfoodPacketText = readFileSync(dogfoodPacketFile, "utf8");
assertContainsAll(dogfoodPacketText, [
  "user-approved Codex PR workflow",
  "Codex may code, test, and open a PR only when the surrounding prompt explicitly scopes that task.",
  "Codex should open a PR, not merge.",
  "ChatGPT reviews PR after Codex opens it.",
  "User decides whether to merge.",
  "Packet does not grant authority by itself.",
  "Summary: omitted for manual ingress packet.",
]);
assert.equal(
  dogfoodPacketText.includes("- Do not execute Codex."),
  false,
  "dogfood artifact must not include the ambiguous standalone Codex execution ban",
);
assertNoForbiddenDogfoodText(dogfoodPacketText, dogfood.preview);

const samplePreview = buildPerspectiveAgentBriefSourcePreview({
  source: "sample:chatgpt",
});
assert.equal(samplePreview.constellation.nodes.length, 7);
assert.equal(samplePreview.constellation.edges.length, 8);
assert.equal(samplePreview.unresolved_tensions.length, 2);

for (const header of [
  "1. Purpose",
  "2. Selected Perspective Material",
  "3. Evidence",
  "4. Unresolved Tensions",
  "5. Next Action Candidates",
  "6. Suggested Use",
  "7. Compact Authority",
  "8. Base Packet Text",
]) {
  assert(
    handoffPacketStructureSmoke.includes(header),
    `existing Perspective Handoff packet section order smoke must still cover ${header}`,
  );
}

assert.equal(
  cockpitText.includes("perspective_agent_brief_handoff_packet"),
  false,
  "product UI must not expose Agent Brief handoff packet JSON",
);
assert.equal(
  cockpitText.includes("Agent Brief Handoff Copy Refine"),
  false,
  "product UI must not render copy-refine artifact text",
);
assert.equal(
  /\brulecraft\b/i.test(cockpitText),
  false,
  "Rulecraft must remain unexposed in product-facing Cockpit UI",
);

assertChangedFileBoundary();
assertNoRuntimePlumbingInNewFiles();

console.log("PASS smoke:perspective-agent-brief-handoff-copy-refine");

function assertNoForbiddenDogfoodText(text, preview) {
  assert.equal(
    text.includes(PERSPECTIVE_MANUAL_AGENT_BRIEF_CODEX_REVIEW_LOOP_INPUT),
    false,
    "dogfood artifact must not include raw manual input verbatim",
  );

  for (const forbiddenValue of getPerspectiveManualAgentBriefCodexReviewLoopForbiddenValues(
    preview,
  )) {
    assert.equal(
      text.includes(forbiddenValue),
      false,
      `dogfood artifact must not include forbidden raw/candidate value: ${forbiddenValue}`,
    );
  }

  for (const forbiddenMarker of getPerspectiveManualAgentBriefCodexReviewLoopForbiddenMarkers()) {
    assert.equal(
      text.includes(forbiddenMarker),
      false,
      `dogfood artifact must not include forbidden marker: ${forbiddenMarker}`,
    );
  }
}

function assertSectionOrder(packetText) {
  const headings = [
    "## Purpose",
    "## Selected Material",
    "## Spatial Context",
    "## Temporal Context",
    "## Ingress Context",
    "## Tensions",
    "## Next Actions",
    "## Handoff Constraints",
    "## Authority",
    "## Exclusions",
  ];
  let previousIndex = -1;
  for (const heading of headings) {
    const index = packetText.indexOf(heading);
    assert.notEqual(index, -1, `packet must include heading ${heading}`);
    assert(index > previousIndex, `packet heading must be ordered: ${heading}`);
    previousIndex = index;
  }
}

function assertChangedFileBoundary() {
  for (const changedFile of collectChangedFiles()) {
    assert(
      allowedChangedFiles.has(changedFile),
      `Agent Brief handoff copy refine changed an out-of-scope file: ${changedFile}`,
    );
    assert(
      !changedFile.startsWith("app/api/") &&
        !changedFile.startsWith("components/") &&
        !changedFile.startsWith("app/globals.css") &&
        !changedFile.startsWith("db/") &&
        !changedFile.startsWith("migrations/") &&
        !changedFile.startsWith("fixtures/") &&
        !changedFile.includes("persistence") &&
        !changedFile.includes("provider") &&
        !changedFile.includes("codex-execution") &&
        !changedFile.includes("oauth"),
      `Agent Brief handoff copy refine must not change forbidden surfaces: ${changedFile}`,
    );
  }
}

function assertNoRuntimePlumbingInNewFiles() {
  const forbiddenMarkers =
    getPerspectiveManualAgentBriefCodexReviewLoopForbiddenMarkers().filter(
      (marker) =>
        ![
          ["input", "text"].join("_"),
          "\"ingress_admission\"",
          "\"brief_version\"",
          "\"candidate_id\"",
          "\"source_ref\"",
          "\"pointer_refs\"",
          "\"actor_refs\"",
          "\"consent_ref\"",
          "\"bounded_summary\"",
          "Perspective Handoff Packet",
          "Graph nodes:",
          "Graph edges:",
          "OAuth token",
          "billing marker",
        ].includes(marker),
    );
  const filesToScan = [
    builderFile,
    dogfoodScriptFile,
    smokeFile,
    docFile,
    reportFile,
    dogfoodPacketFile,
    dogfoodEvalReportFile,
  ];

  for (const file of filesToScan) {
    const text = existsSync(file) ? readFileSync(file, "utf8") : "";
    for (const forbiddenMarker of forbiddenMarkers) {
      assert.equal(
        text.includes(forbiddenMarker),
        false,
        `${file} must not add runtime/provider/token plumbing: ${forbiddenMarker}`,
      );
    }
  }
}

function assertContainsAll(text, snippets) {
  const normalized = normalize(text);
  for (const snippet of snippets) {
    assert(
      normalized.includes(normalize(snippet)),
      `Expected source to contain: ${snippet}`,
    );
  }
}

function collectChangedFiles() {
  const workingTreeFiles = gitLines(["diff", "--name-only", "HEAD"]);
  const branchFiles = gitLines(["diff", "--name-only", "origin/main...HEAD"]);
  const untrackedFiles = gitLines(["ls-files", "--others", "--exclude-standard"]);
  return Array.from(
    new Set([...workingTreeFiles, ...branchFiles, ...untrackedFiles]),
  ).filter(Boolean);
}

function gitLines(args) {
  try {
    return execFileSync("git", args, { encoding: "utf8" })
      .split("\n")
      .map((line) => line.trim())
      .filter(Boolean);
  } catch {
    return [];
  }
}

function normalize(text) {
  return text.replace(/\s+/g, " ").trim();
}
