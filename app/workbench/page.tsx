import { AugnesCockpit } from "@/components/augnes-cockpit";

export const metadata = {
  title: "Augnes Workbench",
  description: "Augnes cockpit workbench",
};

export default function WorkbenchPage() {
  return <AugnesCockpit />;
}
